spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_diary?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 123456
  application:
    name: database-provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
  instance:
    prefer-ip-address: true
server:
  port: 6001
